services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: blank_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
